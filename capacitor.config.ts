import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mariswalegacy.app',
  appName: 'Mari Swa Transcript',
  webDir: 'dist'
};

export default config;
