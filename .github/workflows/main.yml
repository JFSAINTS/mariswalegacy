name: Generar APK de PWA

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build_apk:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Instalar dependencias de la PWA
        run: npm install

      - name: Construir la PWA
        run: npm run build

      - name: Instalar Bubblewrap
        run: npm install -g @bubblewrap/cli

      - name: Configurar Bubblewrap y generar el proyecto Android
        run: |
          # Asegúrate de que el directorio 'build' existe y contiene los archivos de la PWA
          # La URL de la PWA debe ser la que se usará en producción
          bubblewrap init \
            --manifest https://mariswalegacy.lovable.app/manifest.json \
            --output-dir ./android-project \
            --app-name "Mari Swa Transcript" \
            --package-id com.mariswalegacy.app \
            --start-url https://mariswalegacy.lovable.app/ \
            --icon ./dist/icon-512.png \
            --icon-maskable ./dist/icon-512-maskable.png || true
          # El '|| true' es para que no falle si el proyecto ya existe y se intenta inicializar de nuevo

      - name: Construir el APK
        run: |
          cd ./android-project
          bubblewrap build

      - name: Subir el APK como artefacto
        uses: actions/upload-artifact@v4
        with:
          name: mari-swa-transcript-apk
          path: ./android-project/app/build/outputs/apk/debug/app-debug.apk
